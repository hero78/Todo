
# DATASOURCE
spring.datasource.jdbcUrl=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:todoz}
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:todoz}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:r00t@am1}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.database-platform=org.hibernate.dialect.MySQL5Dialect
spring.jpa.generate-ddl=false
# NONE = disable Hibernate from creating Entity Modals
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.h2.console.isActive=true
#
#
# LIQUIBASE
spring.liquibase.change-log=classpath:liquibase/todo.xml

# SERVER PORT
server.port=8181


